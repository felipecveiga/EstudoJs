// Criar uma função para calcular a 
//porcentagem de um número.

let num, Qporc, resultado
num = prompt("Digite seu numero")
Qporc = prompt("Digite o percentual")
    function porcent(num, Qporc){
    resultado = (num*Qporc)/100
    return alert("o resultado é " + resultado )
}
porcent(num,Qporc)
